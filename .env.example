# At least 32 bytes. Generate once, store it in Vercel, and back it up.
# Rotating this secret invalidates every existing custom feed URL.
CUSTOM_FEED_SIGNING_SECRET=

# Optional intentional override. Permanent production feeds should use the default.
# NEXT_PUBLIC_PODCAST_PACER_ORIGIN=http://localhost:3000
